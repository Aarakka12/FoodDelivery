<div class="row full-height">
<%=render partial:'layouts/category', locals: { categories:@categories, restaurant:@restaurant } %>
 <div class="col s9 full-height scroll-bar">
   <div class="panel header-panel flow-text z-depth-3"><%= (!@category.nil?) ? @category.title : "All cuisines"  %></div>
  <div class="row row-cols-1 row-cols-md-3 g-4"> <% @foods = (!@category.nil?) ? @categories.foods : @foods  %>
  <% @foods.each do |food| %>
      <div class="col">
        <div class="card">
          <%# <div class="card-image">
          
            <span class="activator"><i class="material-icons right">more_vert</i></span> %>
            <% if food.image.attached? %>
              <%# <div class='col-lg-4'> %>
                <%= image_tag food.image, class:"card-img-top" %>
                <%# <span class="card-title"><%= food.name</span> %>
              <%# </div> %>
            <% end %>
          <%# </div> %>
          <%# <div class="card text-center"> %>
          <div class="card-body text-center">
            <h5 class="card-title"><%= food.name %></h5>
            <p><%= food.price%></p>
            <p><%= food.description%></p>
          
            <div class="right"><i class="material-icons left no-right-margin">shopping_order</i><%#= link_to orders_path, class: "order left no_left_pad no-right-margin", id: "order" %></div> 
              <%#= link_to "Add to order", cart_items_path, method: :post, class:"btn btn-primary"%>
              <%= button_to "Add to Cart", carts_path(food_id: food.id), method: :post, class: "btn btn-primary" %>
              <%# <% @cart.inspect %> 
          </div>
          <%# </div> %>
          <%# <div class="card-action"> %>
      
          <%# </div> %>
        </div>
      </div>

     

  <% end %>
 
 </div>
</div>
<%# order_items_path(food_id: food.id, quantity: 1) %>